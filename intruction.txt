zk流程：
1.初始化zk的连接
  加载永久的根目录 root_lock

2.获得锁
  创建有序的临时节点（current_lock）
  获取所有的子节点，并排序
  获取第一个子节点，是否等于当前的current_lock
  不等于的话，就直接利用二分查找当前的前一个节点，wait_lock

3.获取锁失败，直接进行等待
  判断是否存在，存在就等待
  申请countDownLatch（在监听的过程中，process（）{从这里进行释放}）

4.释放锁
  删除当前的节点
  关闭当前的连接







